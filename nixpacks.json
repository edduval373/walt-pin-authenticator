{
  "providers": ["node"],
  "phases": {
    "setup": {
      "nixPkgs": ["nodejs_18", "npm"]
    },
    "install": {
      "cmds": ["npm ci --omit=dev"]
    },
    "build": {
      "cmds": ["echo 'Production server deployment - no build required'"]
    },
    "start": {
      "cmd": "node index.js"
    }
  }
}